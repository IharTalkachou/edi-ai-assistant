# ИИ-помощник EDI Enterprise AI v 0.2

Система интеллектуального анализа электронных накладных (EDI) с использованием локальных LLM (Llama-3), RAG (Векторный поиск) и асинхронной обработки.

## 🚀 Возможности
- **AI-Валидация:** Автоматический анализ ошибок в XML-документах.
- **RAG (База знаний):** Поиск релевантных правил валидации через векторный поиск (`pgvector`).
- **Event-Driven:** Асинхронная обработка через Celery + Redis (API не блокируется).
- **Human-in-the-Loop:** Динамическое обновление промптов и правил через БД без перезагрузки.

## 🛠 Технологический стек
- **Core:** Python 3.10, FastAPI, Pydantic, SQLAlchemy.
- **AI:** Llama-3 (GGUF via `llama-cpp-python`), SentenceTransformers (`all-MiniLM-L6-v2`).
- **Infra:** Docker Compose, PostgreSQL (pgvector), Redis.

## Быстрый старт

### 1. Предварительные требования
- Docker Desktop (запущенный).
- Python 3.10+.
- 5 ГБ места на диске (для модели).

### 2. Установка
1. Клонируйте репозиторий.
2. Скачайте модель Llama-3:
   - Скачайте файл `Meta-Llama-3-8B-Instruct.Q4_K_M.gguf`.
   - Положите его в папку `models/` в корне проекта.
3. Создайте виртуальное окружение:
   ```bash
   python -m venv .venv
   .venv\Scripts\activate  # Windows
   pip install -r requirements.txt